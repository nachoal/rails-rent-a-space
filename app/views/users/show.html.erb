<div class="row">
  <div class="col-md-6">
    <h3><%= @user.email %></h3>
    <h4>My rented spaces:</h4>
    <ul>
      <% if @user.rentals.any? %>
        <% @user.rentals.each do |rental| %>
        <li><%= rental %></li>
        <% end %>
      <% else %>
        <p>You don't have any rented spaces yet. You should rent one! </p>
      <% end %>
    </ul>
    <% if @user.spaces.any? %>
      <h3>Places that you have for rent:</h3>
      <ul>
        <% @spaces.each do |space| %>
          <li><p><strong>Address:</strong><%= space.address %> <strong>| Description:</strong> <%= space.description %> <strong>Country:</strong><%= space.country%> </p></li>
        <% end %>
      </ul>
    <% end  %>
  </div>
  <div class="col-md-6">

      <h3>Add a new place for rent:</h3>
      <%= simple_form_for [@user, @space] do |f| %>
        <%= f.input :name %>
        <%= f.input :address %>
        <%= f.input :city %>
        <%= f.input :country %>
        <%= f.input :capacity %>
        <%= f.input :description %>
        <%= f.input :price %>
        <%= f.input :photo %>
        <%= f.input :photo_cache, as: :hidden %>
        <%= f.button :submit, class: "btn btn-success" %>
      <% end %>

  </div>
</div>
